language: java
sudo: false

jdk:
  - oraclejdk8
services:
  - docker
cache:
  directories:
  - $HOME/.m2
install:
 - docker pull mongo

script:
- mvn -f pom.xml clean verify


notifications:
email: false